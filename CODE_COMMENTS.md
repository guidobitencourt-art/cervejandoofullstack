Índice de comentarios en el código
=================================

Este archivo lista dónde se añadieron comentarios explicativos o dónde ya existen dentro del proyecto.

- `src/server/api.ts` — comentarios que describen rutas, conexión a la BD y la lógica de fallback en desarrollo.
- `src/server/middleware/auth.ts` — comentarios que explican el middleware de autenticación JWT.
- `src/server/models/User.ts` — comentario de cabecera breve (modelo para usuarios).
- `scripts/seed-users.cjs` — el script contiene comentarios que explican su uso y lo que crea.
- `README_DEPLOY.md` — contiene la lista de verificación para despliegue y pasos de verificación.

Si necesitas más comentarios en línea en archivos específicos (componentes, hooks o páginas), indícame cuáles y añadiré comentarios explicativos breves sobre funciones y lógica no evidente.
Code comments index
===================

This file lists where explanatory comments were added or already exist within the project.


If you need more inline comments in specific files (components, hooks or pages), tell me which files and I will add short explanatory comments above functions and non-obvious logic.

```markdown
Índice de comentarios en el código
=================================

Este archivo lista dónde se añadieron comentarios explicativos o dónde ya existen dentro del proyecto.

- `src/server/api.ts` — comentarios que describen rutas, conexión a la BD y la lógica de fallback en desarrollo.
- `src/server/middleware/auth.ts` — comentarios que explican el middleware de autenticación JWT.
- `src/server/models/User.ts` — comentario de cabecera breve (modelo para usuarios).
- `scripts/seed-users.cjs` — el script contiene comentarios que explican su uso y lo que crea.
- `README_DEPLOY.md` — contiene la lista de verificación para despliegue y pasos de verificación.

Si necesitas más comentarios en línea en archivos específicos (componentes, hooks o páginas), indícame cuáles y añadiré comentarios explicativos breves sobre funciones y lógica no evidente.

```
